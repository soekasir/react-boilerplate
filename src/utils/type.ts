export namespace Type {

  export type Point = {
    x: number;
    y: number;
  };

  export type RectPoint = {
    x1: number;
    y1: number;
    x2: number;
    y2: number;
  };

  export type Size = {
    width: number;
    height: number;
  };

}